{
  "name": "pokedex-javascript-vanilla",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "rm -rf dist && vite build",
    "test": "vitest",
    "coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-c8": "^0.25.2",
    "sass": "^1.56.1",
    "vite": "^3.2.3",
    "vitest": "^0.25.2"
  },
  "dependencies": {
    "bootstrap": "^5.2.3"
  }
}
